import React, { FC } from "react";

//Components

// Interface's

const Footer: FC = () => {
  return (
    <footer className="col-span-2 border border-red-700  bg-white dark:bg-slate-800">
      <p className="text-sm">footer</p>
    </footer>
  );
};

export default Footer;
